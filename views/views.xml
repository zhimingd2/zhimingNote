<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="zhiming_note.list">
      <field name="name">zhimingNote list</field>
      <field name="model">zhiming_note.zhiming_note</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="zhiming_note.action_window">
      <field name="name">zhimingNote window</field>
      <field name="res_model">zhiming_note.zhiming_note</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="zhiming_note.action_server">
      <field name="name">zhimingNote server</field>
      <field name="model_id" ref="model_zhiming_note_zhiming_note"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="zhimingNote" id="zhiming_note.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="zhiming_note.menu_1" parent="zhiming_note.menu_root"/>
    <menuitem name="Menu 2" id="zhiming_note.menu_2" parent="zhiming_note.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="zhiming_note.menu_1_list" parent="zhiming_note.menu_1"
              action="zhiming_note.action_window"/>
    <menuitem name="Server to list" id="zhiming_note" parent="zhiming_note.menu_2"
              action="zhiming_note.action_server"/>
    -->
  </data>
</odoo>